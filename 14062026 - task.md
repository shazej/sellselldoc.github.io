Listed directory api
Listed directory search
Listed directory search
Viewed route.ts:1-120
Viewed route.ts:120-300
Viewed route.ts:300-600

I now have a complete picture. Here is the full report:

---

## Buyer Search Entry Routes

There are **two distinct buyer search entry routes**, not one. They are independent code paths with different logic.

---

### Route 1 — Explore / Browse Page Search (keyword bar)

**File:** [`src/app/api/products/search/route.ts`](file:///Users/shazej/Documents/askar/1705/pickpick-slsl/src/app/api/products/search/route.ts)

**Endpoint:** `GET /api/products/search?q=...`

**Function Flow (line by line):**

```
export const GET (line 210)
  │
  ├─ 1. Parse query params: q, page, limit, country, category, minPrice, maxPrice, brand, model, demo
  │
  ├─ 2. UUID / Product Code shortcut (lines 230–332)
  │       If q is a UUID or product code → direct prisma lookup → return immediately
  │
  ├─ 3. parseFiltersFromText('', q) → smart (line 337)
  │       Deterministic rule-based filter inference (category, price range)
  │
  ├─ 4. Category resolution (lines 342–352)
  │       prisma.category.findUnique → expand to child slugs
  │
  ├─ 5. parseQuery(q) → parsedQuery (line 360)
  │       Extracts: brand, model, year, color, transmission, fuelType
  │
  ├─ 6. Vehicle exact SQL path (lines 397–700)
  │       Triggered when: isVehicleQuery && hasVehicleFilter
  │       Builds AND conditions for brand + model + year + color
  │       Queries prisma.product.findMany with all conditions
  │       Post-filters by color (in-memory, lines 535–558)
  │       → Returns immediately if results found
  │
  ├─ 7. Qdrant semantic search (lines ~700–800)
  │       expandQueryWithOntology(q) → enriched query text
  │       getTextEmbedding(q) → 768-dim vector (Ollama nomic-embed-text, 5s timeout)
  │       textToSparseVector(q) → BM25 sparse vector
  │       searchProducts(embedding, sparse, filters, limit) → Qdrant hybrid search
  │       → If Qdrant returns 0 IDs → fall through to SQL fallback
  │
  ├─ 8. Prisma re-hydration (fetch full product rows by Qdrant IDs)
  │       computeProductRankScore() → multi-signal ranking
  │       Filter by relevance ≥ 0.20
  │       → Returns if mapped results exist
  │
  └─ 9. sqlTextSearch() fallback (line 48)
          Full-text OR search across title, description, attributes
          computeProductRankScore() + sponsored injection
          → Always returns (even if empty)
```

---

### Route 2 — Chat Interface Search (conversational)

**File:** [`src/app/api/chat/route.ts`](file:///Users/shazej/Documents/askar/1705/pickpick-slsl/src/app/api/chat/route.ts)

**Endpoint:** `POST /api/chat`

**Function Flow:**

```
export async function POST (line 274)
  │
  ├─ 1. Parse body: messages, message, session_id, image_url, locale, product_id, location
  │
  ├─ 2. Session state load: getChatSession(sessionId)
  │
  ├─ 3. Greeting / cancel / negotiation guard (lines 319–391)
  │       Deterministic intercepts — short-circuit before any search
  │
  ├─ 4. Sell flow lock (lines 395+)
  │       If sessionState.mode === 'sell' → routed to sell pipeline, no search
  │
  ├─ 5. Buyer intent detection → extractIntentDeterministic(query)
  │       Pure rule-based extraction (no LLM):
  │       brand, model, color, year, category, price range, location, tags
  │
  └─ 6. searchWithCascade(intent, { page, limit, countryCode })
              │
              ├─ Tier 1: Exact SQL — buildTier1Where() → brand AND model AND color AND category
              │           (only if confidence='high' and brand/model present)
              │
              ├─ Tier 2: Relaxed SQL — buildTier2Where() → any single signal
              │           (runs if any signal exists and Tier1 < 10 results)
              │
              ├─ Tier 3: Qdrant semantic — embedding + BM25 hybrid
              │           SKIPPED if Tier1 + Tier2 ≥ 3 results
              │
              └─ Tier 4: Similar / broader results (no category lock)
```

---

### Summary Table

| | Route 1 (Explore) | Route 2 (Chat) |
|---|---|---|
| **File** | `src/app/api/products/search/route.ts` | `src/app/api/chat/route.ts` |
| **Method** | `GET` | `POST` |
| **Intent extraction** | `parseFiltersFromText()` + `parseQuery()` | `extractIntentDeterministic()` |
| **LLM involved?** | `parseFiltersFromText` can call Ollama | No — purely deterministic |
| **Vehicle exact SQL** | ✅ Has its own vehicle-specific SQL block | ✅ Via Tier 1/2 cascade |
| **Color post-filter** | ✅ In-memory filter (lines 535–558) | ⚠️ Only as rank boost |
| **Qdrant semantic** | ✅ Always attempted | ✅ Attempted (skipped if SQL ≥ 3) |
| **SQL fallback** | ✅ `sqlTextSearch()` | ✅ Falls back to SQL |